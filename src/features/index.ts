import { JobListContainer, JobListReducer, JobListSaga } from "./JobList";
import LoadingContainer from "./Loading"
export const containers = {
  JobListContainer,
  LoadingContainer
};


export const reducers = {
  JobListReducer
};

export const sagas = {
  JobListSaga
};